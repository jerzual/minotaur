import { describe, it, expect } from 'vitest';

import React, { FunctionComponent } from "react";
import Scene from './Scene';

describe('views/Scene', () => {
    it('Display the 3D scene on the canvas.', () => {
        expect(true).toBe(true);
    });
});