import { describe, it, expect } from 'vitest';

/**
 * Hub is in charge of displaying hero life/mana and active weapon/inventory items.
 */
describe('views/Hub', () => {
    it('can be rendered', () => {
        expect(true).toBe(true);
    });
});