import React, { FunctionComponent } from "react";
import Options from './Options';

describe('views/Options', () => {
    beforeEach(() => {
        
    })
    test('moves left', () => {

    });
});