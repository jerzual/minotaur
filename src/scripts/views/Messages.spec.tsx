import { describe, it, expect } from 'vitest';

import React, { FunctionComponent } from 'react';
import Messages from './Messages';

describe('views/Messages', () => {
	it('display player messages', () => {
		expect(true).toBe(true);
	});
});
